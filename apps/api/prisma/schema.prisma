generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id String @id @db.Uuid
  email String @unique
  name String
  password String
  role Role
  imgUrl String?
  createdAt DateTime
  updatedAt DateTime
}

model RegistrationToken {
  id String @id @db.Uuid
  token String
}

model Equipment {
  id String @id @db.Uuid
  name String @unique
  createdAt DateTime
  updatedAt DateTime
}

enum Role {
  USER
  ADMIN
}